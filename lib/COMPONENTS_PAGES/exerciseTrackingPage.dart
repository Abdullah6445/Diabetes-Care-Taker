import 'package:flutter/material.dart';

class exerciseTrackingPage extends StatelessWidget {
  const exerciseTrackingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text("exercise Tracking Page")),
      ),
    );
  }
}
