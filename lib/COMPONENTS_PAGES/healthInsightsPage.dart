import 'package:flutter/material.dart';

class healthInsightsPage extends StatelessWidget {
  const healthInsightsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text("health Insights Page")),
      ),
    );
  }
}
