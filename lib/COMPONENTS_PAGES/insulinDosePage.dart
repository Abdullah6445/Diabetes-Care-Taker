import 'package:flutter/material.dart';

class insulinDosePage extends StatelessWidget {
  const insulinDosePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text("insulin Dose Page")),
      ),
    );
  }
}
